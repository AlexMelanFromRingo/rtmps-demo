# 📡 RTMP Streaming Server

Простой RTMP сервер для стриминга с веб-интерфейсом - ваш личный мини-Twitch!

## 🎯 Возможности

- ✅ **RTMP сервер** для приема потока от OBS Studio
- ✅ **Генерация ключей трансляции** через веб-интерфейс
- ✅ **HLS конвертация** для просмотра в браузере
- ✅ **Веб-плеер** для просмотра трансляций в реальном времени
- ✅ **Управление трансляциями** - создание, просмотр, удаление ключей
- ✅ **Статусы в реальном времени** - онлайн/оффлайн индикаторы

## 🚀 Быстрый старт

### Требования

- Node.js (версия 14 или выше)
- FFmpeg (для конвертации RTMP → HLS)

### Установка FFmpeg

**Ubuntu/WSL2:**
```bash
sudo apt update
sudo apt install ffmpeg
```

**Windows:**
- Скачайте FFmpeg с официального сайта: https://ffmpeg.org/download.html
- Добавьте путь к FFmpeg в системную переменную PATH

**macOS:**
```bash
brew install ffmpeg
```

### Установка и запуск

1. **Клонируйте репозиторий:**
```bash
git clone <your-repo-url>
cd rtmps-demo
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Запустите сервер:**
```bash
npm start
```

4. **Откройте веб-интерфейс:**
```
http://localhost:3000
```

## 📝 Как использовать

### 1. Генерация ключа трансляции

1. Откройте http://localhost:3000 в браузере
2. Введите название трансляции (опционально)
3. Нажмите "Создать ключ трансляции"
4. Скопируйте **RTMP URL** и **Ключ потока**

### 2. Настройка OBS Studio

1. Откройте **OBS Studio**
2. Перейдите в **Настройки → Трансляция**
3. В поле "Служба" выберите **Пользовательский...**
4. Вставьте данные:
   - **Сервер:** `rtmp://localhost:1935/live`
   - **Ключ потока:** `<ваш сгенерированный ключ>`
5. Нажмите **OK**
6. Нажмите **Начать трансляцию** в главном окне OBS

### 3. Просмотр трансляции

1. На главной странице нажмите кнопку "🚀 Открыть" рядом с ключом
2. Или перейдите по ссылке для просмотра
3. Трансляция начнет воспроизводиться автоматически

## 🔧 Технические детали

### Архитектура

```
OBS (RTMP, H.264)  → node-media-server (1935) ┐
OBS (SRT, любой кодек) → ffmpeg listener (9000+) ┤→ ffmpeg → ABR HLS (1080p/720p/480p)
                                                  │         master.m3u8 + варианты
                                                  ↓
                              Express (3000): дашборд + API + раздача /media (HLS)
```

FFmpeg перекодирует вход в адаптивную лестницу H.264 (1080p/720p/480p) и пишет
`master.m3u8` со ссылками на варианты. Плеер берёт HLS с того же origin (3000),
поэтому CORS не нужен. На GPU NVIDIA автоматически используется NVENC.

### Используемые технологии

- **Backend:**
  - Node.js
  - Express - веб-сервер и API
  - node-media-server - RTMP сервер
  - FFmpeg - конвертация RTMP в HLS

- **Frontend:**
  - Vanilla JavaScript
  - Video.js - HTML5 видеоплеер с HLS поддержкой
  - CSS3 с градиентами и анимациями

### Порты

- **3000** - Веб-интерфейс, API и раздача HLS (`/media`)
- **1935** - RTMP сервер (ingest)
- **8000** - node-media-server HTTP/FLV (резервный origin)
- **9000+** - SRT-листенеры (по одному порту на ключ)

Все порты, хост и пути настраиваются через переменные окружения — см. «Конфигурация».

## 📖 API Endpoints

### POST `/api/generate-key`
Создает новый ключ трансляции

**Body:**
```json
{
  "name": "My Stream"
}
```

**Response:**
```json
{
  "streamKey": "uuid",
  "name": "My Stream",
  "rtmpUrl": "rtmp://localhost:1935/live",
  "rtmpStreamKey": "uuid",
  "srtUrl": "srt://localhost:9000?mode=caller",
  "srtPort": 9000,
  "hlsUrl": "http://localhost:3000/media/live/uuid/master.m3u8",
  "webPlayerUrl": "http://localhost:3000/watch.html?key=uuid",
  "protocols": { "rtmp": { "...": "..." }, "srt": { "...": "..." } }
}
```

### GET `/api/keys`
Получить список всех ключей

### GET `/api/key/:key`
Получить информацию о конкретном ключе

### DELETE `/api/key/:key`
Удалить ключ (только если трансляция не активна)

## 🎨 Скриншоты

### Главная страница
- Генерация ключей трансляции
- Список активных и неактивных трансляций
- Статусы в реальном времени

### Страница просмотра
- HTML5 видеоплеер
- Индикатор статуса трансляции
- Информация о трансляции

## 🛠️ Разработка

### Режим разработки с автоперезагрузкой:
```bash
npm run dev
```

### Структура проекта
```
rtmps-demo/
├── server.js           # RTMP/SRT/HTTP сервер, API, жизненный цикл ffmpeg
├── config.js           # Конфигурация из переменных окружения
├── lib/
│   └── encoding.js     # Детект энкодера + генерация ffmpeg-аргументов ABR HLS
├── test/
│   ├── encoding.test.js # Юнит-тесты генератора аргументов
│   └── server.test.js   # E2E: API + реальный RTMP/SRT → HLS
├── package.json        # Зависимости и npm-скрипты
├── public/             # Статические файлы (дашборд + плеер)
│   ├── index.html
│   ├── watch.html
│   ├── style.css
│   └── app.js
└── media/              # HLS-выход (генерируется автоматически)
```

## ⚙️ Конфигурация

Все настройки задаются через переменные окружения (значения по умолчанию — для
локального запуска):

| Переменная        | По умолчанию | Назначение                                  |
|-------------------|--------------|---------------------------------------------|
| `PUBLIC_HOST`     | `localhost`  | Хост в генерируемых URL (LAN/публичный IP)  |
| `WEB_PORT`        | `3000`       | Дашборд, API, раздача HLS                   |
| `RTMP_PORT`       | `1935`       | RTMP ingest                                 |
| `MEDIA_HTTP_PORT` | `8000`       | node-media-server HTTP/FLV                   |
| `SRT_BASE_PORT`   | `9000`       | Первый порт для SRT-листенеров              |
| `DB_PATH`         | `./streaming.db` | Путь к базе SQLite                       |
| `MEDIA_ROOT`      | `./media`    | Каталог HLS-выхода                          |
| `FFMPEG_PATH`     | `ffmpeg`     | Путь к бинарю ffmpeg                         |

Пример (стриминг по локальной сети):
```bash
PUBLIC_HOST=192.168.1.50 npm start
```

## 🧪 Тесты

```bash
npm test          # всё (юнит + e2e)
npm run test:unit # только быстрые юнит-тесты
npm run test:e2e  # e2e: поднимает сервер и реально гонит RTMP/SRT → HLS
```

E2E-тестам нужен установленный `ffmpeg` (используется и как тестовый источник).
Они работают на изолированных портах и временной БД и ничего не оставляют после себя.

## 🐛 Устранение неполадок

### Трансляция не запускается в OBS
- Убедитесь, что сервер запущен
- Проверьте правильность RTMP URL и ключа
- Проверьте логи сервера на наличие ошибок

### Видео не воспроизводится в браузере
- Подождите несколько секунд после начала трансляции (FFmpeg создает HLS сегменты)
- Обновите страницу
- Проверьте консоль браузера на ошибки

### FFmpeg не найден
- Убедитесь, что FFmpeg установлен: `ffmpeg -version`
- В WSL2: `which ffmpeg` должен показать `/usr/bin/ffmpeg`
- При необходимости измените путь в `server.js` (config.trans.ffmpeg)

## 🌐 Использование в сети

Для доступа из сети (не только localhost):

1. Измените `localhost` на ваш IP адрес во всех URL в коде
2. Откройте порты 1935, 3000, 8000 в файрволе
3. Используйте ваш внешний IP в настройках OBS

**Для WSL2:**
```bash
# Узнать IP адрес WSL
hostname -I
```

## 📚 Дополнительная информация

### Про RTMP
RTMP (Real-Time Messaging Protocol) - протокол потоковой передачи данных, разработанный Macromedia (теперь Adobe). Работает поверх TCP, стандартный порт - 1935.

### Про HLS
HLS (HTTP Live Streaming) - протокол потоковой передачи от Apple. Разбивает видео на небольшие сегменты (.ts файлы) и создает плейлист (.m3u8), который может воспроизводиться в любом современном браузере.

### Почему RTMP → HLS?
- RTMP не поддерживается современными браузерами (требует Flash)
- HLS работает через HTTP и поддерживается всеми браузерами
- OBS отправляет RTMP (низкая задержка)
- Браузер получает HLS (совместимость)

## 📄 Лицензия

MIT

## 🤝 Вклад в проект

Pull requests приветствуются! Для больших изменений сначала откройте issue для обсуждения.

---

**Создано с ❤️ для изучения основ стриминга**
