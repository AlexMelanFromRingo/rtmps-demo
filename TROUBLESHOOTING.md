# 🔧 Решение проблем RTMP сервера

## ❌ Ошибка: "Video codec not implemented"

### Симптомы:
```
[flv @ 0x...] Video codec (d) is not implemented
Stream #0:1: Video: none ([13][0][0][0] / 0x000D)
```

### Причина:
OBS отправляет видео в кодеке **AV1** (0x0D), который RTMP/FLV протокол не поддерживает.

### ✅ Решение:

#### 1. Откройте настройки OBS
**Файл → Настройки** (или Settings)

#### 2. Перейдите в раздел "Вывод" (Output)

#### 3. ОБЯЗАТЕЛЬНО: измените режим на "Расширенный"
- **Режим вывода**: `Расширенный` (Advanced)

  > **Важно**: В режиме "Простой" нет всех нужных настроек!

#### 4. Вкладка "Трансляция" (Streaming)

Найдите настройку **"Кодировщик видео"** (Video Encoder).

**Текущее значение (НЕПРАВИЛЬНО):**
- ❌ `AV1 NVENC`
- ❌ `SVT-AV1`
- ❌ `HEVC NVENC`
- ❌ `AMD AV1`

**Измените на (ПРАВИЛЬНО):**
- ✅ `NVIDIA NVENC H.264 (new)` ← **Для RTX 4080 Super**
- ✅ `NVIDIA NVENC H.264`
- ✅ `x264` (если нет NVENC)

#### 5. Дополнительные настройки кодировщика:

После выбора H.264 настройте:
```
Управление битрейтом: CBR
Битрейт: 6000 Kbps
Интервал ключевых кадров: 2
Предустановка: Quality (или Max Quality)
```

#### 6. Применить и перезапустить

1. Нажмите **"Применить"**
2. Нажмите **"OK"**
3. **ПЕРЕЗАПУСТИТЕ OBS** (закройте и откройте заново)
4. Начните трансляцию

---

## 📸 Скриншот правильных настроек

```
┌─────────────────────────────────────────┐
│ Настройки → Вывод                       │
├─────────────────────────────────────────┤
│ Режим вывода: [Расширенный         ▼]  │
│                                         │
│ ┌─── Трансляция ───────────────────┐   │
│ │                                   │   │
│ │ Кодировщик видео:                 │   │
│ │ [NVIDIA NVENC H.264 (new)    ▼] │← │
│ │                                   │   │
│ │ Управление битрейтом: [CBR   ▼]  │   │
│ │ Битрейт: [6000] Kbps             │   │
│ │ Интервал ключевых кадров: [2]     │   │
│ │ Предустановка: [Quality      ▼]  │   │
│ │                                   │   │
│ │ Кодировщик аудио: [AAC       ▼]  │   │
│ │ Битрейт: [192] Kbps              │   │
│ └───────────────────────────────────┘   │
│                                         │
│         [Применить]  [OK]  [Отмена]    │
└─────────────────────────────────────────┘
```

---

## 🔍 Проверка текущего кодека в OBS

### Способ 1: Через настройки
1. Откройте **Настройки → Вывод**
2. Проверьте **"Кодировщик видео"**
3. Должно быть: `NVIDIA NVENC H.264` или `x264`

### Способ 2: Через статистику
1. Начните трансляцию
2. **Вид → Док-панели → Статистика**
3. Найдите строку **"Кодировщик"**
4. Должно быть: `jim_nvenc (h264_nvenc)` или `obs_x264`

### Способ 3: Через логи
1. **Справка → Файлы журналов → Показать файлы журналов**
2. Откройте последний лог
3. Найдите строку с `[ffmpeg muxer: 'rtmp_output']`
4. Проверьте кодек - должно быть `h264`, НЕ `av1`

---

## 🎯 Почему не AV1?

### RTMP/FLV ограничения:

RTMP протокол и FLV контейнер были созданы в 2002 году Adobe и поддерживают только:

| Кодек | FLV ID | Поддержка |
|-------|--------|-----------|
| Sorenson H.263 | 2 | ✅ (устарел) |
| Screen Video | 3 | ✅ (устарел) |
| VP6 | 4 | ✅ (устарел) |
| H.264/AVC | 7 | ✅ **ИСПОЛЬЗУЙТЕ ЭТО** |
| HEVC/H.265 | 12 | ❌ Не стандарт |
| **AV1** | **13** | ❌ **НЕ ПОДДЕРЖИВАЕТСЯ** |

**AV1** - современный кодек (2018), но RTMP его не поддерживает.

**Для локального стриминга** используйте H.264 - он отлично работает и поддерживается везде.

---

## 🚀 Альтернативные решения

### Вариант 1: Использовать SRT вместо RTMP (сложнее)
SRT протокол поддерживает современные кодеки, но требует другую настройку сервера.

### Вариант 2: WebRTC (для будущих версий)
WebRTC поддерживает AV1, VP9, и другие современные кодеки.

### Вариант 3: HLS напрямую (без RTMP)
OBS может выводить напрямую в HLS, минуя RTMP.

**Но для этого проекта**: просто используйте **H.264** - это стандарт и работает везде!

---

## ✅ После исправления

После изменения кодека на H.264 вы должны увидеть в логах сервера:

```
[FFmpeg] Starting transcoding: rtmp://localhost:1935/live/...
Input #0, flv, from 'rtmp://localhost:1935/live/...':
  Stream #0:0: Audio: aac (LC), 48000 Hz, stereo, fltp, 192 kb/s
  Stream #0:1: Video: h264, yuv420p, 1920x1080, 60 fps  ← ЭТО ПРАВИЛЬНО!
```

**НЕ должно быть:**
- ❌ `Video codec (d) is not implemented`
- ❌ `Video: none ([13][0][0][0])`
- ❌ `AV1`

---

## 📝 Краткий чеклист

- [ ] OBS → Настройки → Вывод
- [ ] Режим: **Расширенный**
- [ ] Кодировщик: **NVIDIA NVENC H.264**
- [ ] Битрейт: **6000 Kbps**
- [ ] Интервал ключевых кадров: **2**
- [ ] Применить → OK
- [ ] Перезапустить OBS
- [ ] Начать трансляцию
- [ ] Проверить логи - должно быть `h264`, НЕ `av1`

---

## 🆘 Все еще не работает?

### Проверьте:
1. **Версия OBS**: обновите до последней версии
2. **Драйверы NVIDIA**: обновите драйверы RTX 4080 Super
3. **Перезагрузите OBS** после изменения настроек
4. **Проверьте логи OBS**: Справка → Файлы журналов

### Если NVENC H.264 не появляется в списке:
1. Обновите драйверы NVIDIA
2. Используйте `x264` вместо NVENC (использует CPU)

### x264 настройки (если нет NVENC):
```
Кодировщик: x264
Битрейт: 6000 Kbps
Предустановка CPU: veryfast
Профиль: high
Tune: (none)
```

---

**После правильной настройки стрим должен работать без проблем! 🎉**
